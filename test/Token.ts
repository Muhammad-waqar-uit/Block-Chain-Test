import { ethers } from "hardhat";

const {expect} = require('chai');
describe("Contract QTKN",function(){
  it("Deployement should give total supply to owner",async function() {
    const [owner] = await ethers.getSigners();
    
  })
});